#include <iostream>
#include "Time.h"
using namespace std;

int main()
{
    // This originally ran the constructor with the double seconds, but
    // switched to using the one with all integers when it was added.
    Time current_time(9, 14, 3);
}
